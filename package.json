{
  "name": "battle-ship",
  "version": "1.0.0",
  "private": true,
  "description": "Elegant online multiplayer Battle Ship prototype (React, TypeScript, Node, Socket.io).",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "generate-sounds": "node frontend/scripts/generateSounds.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

